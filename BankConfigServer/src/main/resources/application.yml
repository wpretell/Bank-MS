server:
  port: 8088
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/wpretell/Bank-MS.git
          search-paths: config-data
  application:
    name: Bank-MS
